# 更新记录 / Changelog

历史条目描述对应版本的行为；当前使用方式以 README 和使用指南为准。更早版本见 GitHub Releases。
Historical entries describe each version at the time. See the README for current behavior and GitHub Releases for earlier versions.

## 1.10.4

- 英文名称确定为 **Starveil**，保留中文名“星幕”。统一主面板、设置、托盘、统计导出和 Windows 程序名称。
- 程序继续读取原配置、历史和缓存目录；保留 1.10.3 中确认的内置默认设置，兼容早期中文名称的配置位置。
- Windows 便携包使用 `Starveil-1.10.4-win-x64.exe` 文件名，同时生成 SHA-256 校验文件。
- 整理中英文 README、演示图片、使用与发布说明，明确 douyu-danmaku-overlay 和 douyu-monitor/remix 两个来源，保留原作者版权与完整 MIT 许可，并核验 Windows 程序内的许可文件。
- 增加 Windows 自动检查、构建产物和手动创建 GitHub Release 草稿的流程；更新地址统一读取项目仓库元数据。
- 使用独立的 `qxpdev/Starveil` 发布仓库，更新项目首页、反馈入口和程序更新地址。
- 补齐全新安装与已有依赖布局的打包映射，核验 Excel 导出所需的嵌套依赖。

English: Introduces the Starveil name, keeps existing settings and storage compatible, and prepares Windows packages, checksums, documentation and GitHub build/release workflows.

## 1.10.3

- 将当前保存的整套配置设为程序内置默认值：右侧边缘沉浸、18px 字号、30px 头像、1.0 行高、8 条同屏、440px 阅读宽度、距底部 7%、消息间距 10px，统计栏在底部常驻。
- 礼物默认采用当前双行卡片、透明底色、斗鱼官方礼物条与动画；消息保留 6 秒、礼物顶部额外停留 4 秒，滚动速度 100%。房间号、窗口位置、过滤与统计等设置一并沿用当前保存的值。
- 首次启动或缺少配置时采用内置默认值，“恢复默认排版”使用同一套字号与间距。已有配置继续优先读取，后续保存的修改在重开后保留；同步更新相关设置的默认回退值。

## 1.10.2

- 优化由下而上的弹幕流：柔和起步、中段推进、末段减速，普通消息流与边缘沉浸使用同一套非线性上移动画。
- 连续新消息到来时延续现有速度和加速度，避免反复急起步；先推进当前轨迹再处理本帧新消息。短暂掉帧按实际时间推进，调速、目标反向与停止均避免回弹。
- 边缘横向入场同步采用柔和起步，协调上移与进场节奏，减少新旧文字在动画前段的交叠。
- 保留现有滚动速度设置、关闭动效和系统减少动态效果支持。边缘模式仍从底部即时进入、按顺序一起上移，礼物顶部额外停留独立计时。

## 1.10.1

- 边缘沉浸恢复按接收顺序从底部进入、已有消息一起平滑上移。新消息不等待旧消息的保留时间或离场动画，满屏直接让顶部旧消息退出，不积压待显示队列。
- 移除按文字长短计算阅读时间及对应控件，统一使用“消息保留时间”，默认 6 秒，支持 2–20 秒。
- 恢复“礼物顶部额外停留”，默认 4 秒，支持 0–60 秒。礼物到顶部后独立计时，下方消息继续进入和上移；最多保留一条顶部礼物，后续礼物到顶时接替，不排队。单条显示或空间不足时优先展示最新消息。
- 礼物与弹幕继续按实际高度紧密排列，不预留固定空白。纵向上移和横向进退场分别处理，持续新消息、补价和设置调整不会反复重启动画。
- 保留透明礼物栏、醒目总金额、高能正文接入和与普通弹幕共用排版的粉丝牌升级。旧版礼物顶部停留设置可继续使用，1.10.0 的自动阅读参数在加载时移除。

## 1.10.0

- 边缘沉浸取消待显示队列和礼物顶部追加停留。新消息直接进入空位，满屏时替换最早消息；已显示消息保持原位、各自到期向侧边退出，不因其他消息的阅读时间停住。
- 阅读时长按文字长度估算，可设置最短时间、长文字阅读速度和最长时间。默认至少 6 秒、每秒 7 个中文字符、最长 30 秒；完整进场后才计时。满屏替换优先保持直播即时性，统计与已开启的正文保存仍逐条采集。
- 礼物总金额默认加大、加粗并增加描边，透明底色保留。新增“礼物金额大小”设置，支持 100%–180% 实时调整；抽奖仍明确区分投入和已收到的奖励。
- 粉丝牌升级复用普通弹幕的头像、身份、昵称和正文排版，不增加聊天条数或礼物消费。
- 接入斗鱼官方高能广播 voice_trlt / anchor_voice_trlt。新增记录缺少正文时，按记录 ID 请求官方 batchVoiceDetail 接口的 content，作为带“高能”标记的普通消息显示，可随正文记录保存和按用户查询。忽略初始快照、审核与撤下通知；高能消息不受普通弹幕的 dms 缺失过滤。
- 高能详情请求只使用内存中的访客校验信息，不存登录资料或额外磁盘缓存。只有实际收到新增广播且斗鱼返回正文时才能显示；接口不开放或失败时不生成虚构正文，不重复计入礼物消费。

## 1.9.0

- 边缘沉浸的礼物和弹幕共用一列，按实际到达顺序从底部侧边进入，再一起平滑上移。移除独立礼物区域和固定留白，使用统一的消息间距与同屏总数。
- 礼物到达消息列最上方后，完成正常阅读，再追加停留，默认 4 秒，可设为 0–60 秒。追加停留期间礼物仍占一个位置，后续消息按序等待；新礼物或开奖可延长阅读，仅补价不重播退场。
- 普通礼物默认使用紧凑单行，可切换双行卡片。礼物整体、透明底色和官方礼物条各自有独立不透明度，拖动实时生效；抽奖仍显示投入、已开金额和奖品明细。
- 新增边缘统计栏：隐藏、有消息时显示、始终显示三种方式，可固定在阅读区域顶部或底部。统计栏单独设置字号、整体与底色不透明度，不随消息移动。
- 设置重新整理为显示与窗口、外观与排版、边缘沉浸、消息组成、礼物与抽奖、统计栏、内容过滤、每日统计、存储与缓存、测试与关于。移除的样式预览不再加入设置页。
- 支持选择缓存位置，在所选文件夹内创建专用 `xingmu-cache` 子目录。保存后重启生效，旧缓存位置仍可在确认后清理；不迁移配置、统计和正文。位置不可用时本次回退默认目录并提示，保留已保存的选择。
- 清理确认框列出当前与已知旧缓存位置、各类可删内容和保留数据。自定义路径验证目录归属，拒绝目录链接及已有其他数据的目标，清理只处理明确的缓存类别。

## 1.8.0

- 边缘沉浸采用方案 A：新消息在底部侧边进入，已有消息按时间顺序平滑上移，最旧的一条从阅读列顶部向侧边退出。先腾出位置再进场，横向与纵向动画互不覆盖。
- 进场完成后才开始计算完整停留时间，高峰不再强制缩短到 2.4 秒，也不丢弃仅排队 4 秒的消息。显示队列最多保留最近 200 条；主进程统计与已启用的正文保存独立，不受显示队列容量影响。
- 普通弹幕和礼物分区排队，礼物不占普通弹幕同屏条数。礼物区固定在阅读列下方，最多两条，并按实际可用高度适配；礼物到来不会挤走普通弹幕。普通窗口的“最多条数”不再误裁剪边缘队列。
- 礼物改用紧凑的官方动态图与两行文字。默认沿用透明礼物栏，新增可选“斗鱼官方礼物条”；按广播 `bnidv2` 匹配官方皮肤，不按金额伪造平台等级。
- 支持由广播 `eic` 实际触发的官方 VAP 透明 MP4 特效，以真实 RGB/Alpha 矩形解码，静音且同时只播一个。同一卡片不因连击、补价或调整外观反复播放。SVGA、Spine、VAPX 和未取得专属目录的皮肤继续显示礼物图片。
- 官方动画单项限制 16 MB / 15 秒，按需下载，只使用最多 32 MB 内存缓存，不增加视频磁盘文件。播放结束释放当前 Blob、视频与 GPU，清理或退出时释放内存缓存。图片与礼物条沿用 Chromium 128 MB 预算；清理确认框列明各类资源，配置与历史仍保留。

## 1.7.1

- 所有显示方式均可调整滚动速度：100% 为标准速度，控制连续飘屏、消息流移动和边缘进退场，阅读停留时间独立。边缘进场中调速会从当前位置继续，不重新播放动画。
- 边缘模式增加“边缘上下位置”，拖动即可调整距底部的留白并实时移动，松开保存；左右位置和普通窗口位置继续独立保存。
- 移除设置内的样式预览及专用脚本、样式，保留恢复默认排版和实际弹幕窗口的实时调整。
- 礼物栏与礼物条目使用透明背景，移除填充色块与外框，以金额和细色线表示价值等级。

## 1.7.0

- 新增“边缘沉浸”显示方式：每条弹幕和礼物从屏幕左侧或右侧滑入，平滑减速后停留，再加速退回边缘。空闲时完全透明，自动穿透鼠标，始终隐藏窗口底色与常驻统计栏。
- 在“外观与排版 → 滚动与动效 → 显示方式”选择“边缘沉浸”，可调整左右边缘、2–20 秒停留、1–8 条同屏上限与 280–640px 显示宽度。宽度滑杆支持拖动时实时预览、松开保存。
- 消息按实际高度使用独立空位，退出完成后才补入下一条，已有条目不因增删而上下挤动。高峰会在完整进场及短暂阅读后逐条腾出空位，并跳过过时的显示队列；接收统计和已启用的正文记录照常逐条保存。
- 普通窗口位置与边缘显示宽度独立保存，退出沉浸模式恢复原窗口；继续适配所选显示器、Windows 缩放与任务栏工作区。关闭柔和动效或系统要求减少动态效果时，保留停留与自动消失，取消位移动画。

## 1.6.1

- 礼物卡片改为紧凑的两行布局：昵称与金额、礼物与数量。抽奖投入放在礼物行，已开金额放在昵称行，奖品明细保留在下方，移除大金额区和箭头。
- 头像与整条弹幕垂直居中，身份标识、昵称、正文与表情保持行内居中，设置页的样式预览同步采用相同对齐。
- 外观滑杆拖动时按画面帧率预览，松开后保存；预览只改变外观，不切换房间或统计设置。保存失败时回到已保存的外观，较早保存完成也不会覆盖较新的拖动值。

## 1.6.0

- 修复启动后弹幕窗口不显示：隐藏窗口初始化不再依赖 `requestAnimationFrame`，配置与页面都就绪后才显示，退出前先隐藏。保持已保存的飘屏开关状态；关闭时主页明确提示点击“启动飘屏”。
- 新增斗鱼普通表情、旧数字编码、钻粉梗、钻粉及房间表情显示。普通目录包含 343 项映射；钻粉与房间目录通过官网接口更新。84452 房间已核对 15 个钻粉梗表情。
- “消息与礼物”增加“显示斗鱼表情”开关，默认开启。图片固定尺寸占位，重复弹幕折叠和颜色更新保留已有图片节点；未知编码和加载失败回退原文。
- 新增“存储与缓存”页，显示缓存类别、占用、目录与已保存的统计和弹幕占用。清理前弹出 Windows 确认框，列出可删除内容和始终保留的内容，默认选中取消。
- 缓存集中在账户数据目录的 `cache` 下。图片资源由 Chromium 按需缓存，预算 128 MB；主播头像最多 8 MB / 64 个，表情目录最多 4 MB / 32 个房间。头像和目录保留不超过 7 天，表情目录每 6 小时检查更新，接口暂不可用时保留已有目录。
- 只清理明确列出的缓存文件，拒绝目录链接、越界路径和扫描后发生变更的文件。占用中的文件会跳过并反馈实际结果。Windows 下旧 `Cache` 与新 `cache` 同名，旧网页缓存仅清理 `Cache/Cache_Data`。
- 配置、备份、手动补价、统计、用户与礼物记录、保存的弹幕正文、导出的 Excel 和其他非缓存文件不在清理范围内。
- 移除非 Windows 运行分支和独立的 Remix 参考项目，保留第三方许可。构建仅输出 Windows 便携程序，成功打包后自动清理打包中间目录。
- 构建前清除旧的哈希脚本与样式，避免反复构建后把旧代码一同打包；中文项目路径也会核验清理结果。
